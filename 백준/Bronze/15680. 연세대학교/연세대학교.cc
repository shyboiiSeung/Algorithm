#include<stdio.h>

int main() {
    int N;
    scanf("%d", &N);
    if (N == 1) {
        printf("Leading the Way to the Future");
    }
    else {
        printf("YONSEI");
    }
    return 0;
}